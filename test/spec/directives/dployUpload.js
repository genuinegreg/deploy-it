'use strict';

describe('Directive: dployUpload', function() {
  beforeEach(module('deployItApp'));

  var element;

//  it('should make hidden element visible', inject(function($rootScope, $compile) {
//    element = angular.element('<dploy-upload></dploy-upload>');
//    element = $compile(element)($rootScope);
//    expect(element.text()).toBe('this is the dployUpload directive');
//  }));
});
